import BudgetHeader from '../budget/header/budgetHeader';

const BudgetsTab: React.FC = () => {
  return (
    <div className="px-[16px] py-[24px] lg:py-[32px] lg:px-[40px]">
      <BudgetHeader />
    </div>
  );
};

export default BudgetsTab;
